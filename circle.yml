machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - npm install -g slot-cli@"~0.0.17-alpha"

test:
  post:
    - npm run coveralls

general:
  branches:
    only:
      - master
      - develop